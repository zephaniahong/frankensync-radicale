[server]
hosts = [::]:3000

[auth]
type = none
htpasswd_filename = /data/users
htpasswd_encryption = bcrypt

[storage]
filesystem_folder = /data/collections

[logging]
level = info

[rights]
type = owner_only

[web]
type = internal 